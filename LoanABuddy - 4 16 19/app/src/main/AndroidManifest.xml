<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loanabuddy">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View_Loan"></activity>
        <activity android:name=".View_Request" />
        <activity android:name=".My_Request" />
        <activity android:name=".Verify_Request" />
        <activity android:name=".Create_Loan" />
        <activity android:name=".Create_Borrow" />
        <activity android:name=".Create_Request" />
        <activity android:name=".Home_Screen" />
        <activity android:name=".Pay_Settings" />
        <activity android:name=".Transactions" />
        <activity android:name=".Add_Credit_Card"/>
        <activity android:name=".Payout"/>
        <activity android:name=".Personal_Info"/>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>